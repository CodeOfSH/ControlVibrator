<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".MainActivity"
    android:layout_gravity="center">

        <Button
            android:id="@+id/btn_select"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="@string/btn_select"
            android:layout_above="@id/btn_once"
            android:layout_centerHorizontal="true"
            android:onClick="selectFile"/>
        <Button
            android:id="@+id/btn_once"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="@string/btn_once"
            android:layout_centerInParent="true"
            android:onClick="vibrateOnce"/>
        <Button
            android:id="@+id/btn_repeat"
            android:layout_width="200dp"
            android:layout_height="50dp"
            android:text="@string/btn_repeat"
            android:layout_below="@+id/btn_once"
            android:layout_centerHorizontal="true"
            android:onClick="vibrateRepeat"/>

</RelativeLayout>